spring:
  datasource:
    url: jdbc:mariadb://192.168.3.31:3306/netx
    username: dev
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
   
    properties:
      hibernate:
        '[format_sql]': true
